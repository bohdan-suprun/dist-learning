spring:
  application:
     name: test-service

server:
  port: 8020
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
  instance:
    leaseRenewalIntervalInSeconds: 5

security:
  basic:
    enabled: false